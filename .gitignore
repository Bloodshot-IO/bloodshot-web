# Dependencias / build (por si acaso)
/node_modules
/vendor

# Entorno y secretos
.env
*.local.php

# Sistema
.DS_Store
Thumbs.db

# Editor
/.vscode
/.idea

# Entorno de staging (vive en el servidor, no en el repo)
/staging